name: Build Smart Clinic APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ==========================================
      # 🌟 هذه هي الخطوة السحرية التي تحل المشكلة
      # ==========================================
      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable' # نستخدم النسخة المستقرة من فلاتر

      - name: 📦 Install Requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Build APK with Flet
        run: flet build apk --product "Smart Clinic" --org "com.yourname.smartclinic"

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Smart-Clinic-App
          path: build/apk/**/*.apk
